# Database Guidelines

> Not applicable to this project.

---

## Overview

TAP has no database. All data is fetched at runtime via HTTP (`fetch` steps) or Chrome DevTools Protocol (`navigate` + `evaluate` steps). There is no persistence layer.

If a future feature requires persistence, create a new guideline document at that time.
